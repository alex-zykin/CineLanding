[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cinelanding"
version = "0.1.0"
description = "Agent-first toolkit for cinematic scroll-driven landing pages"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "AGPL-3.0-or-later" }
authors = [{ name = "Alex Zykin" }]
dependencies = []

[project.urls]
Homepage = "https://cinelanding.ru"
Repository = "https://github.com/alex-zykin/CineLanding"

[project.scripts]
cinelanding = "cinelanding.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
